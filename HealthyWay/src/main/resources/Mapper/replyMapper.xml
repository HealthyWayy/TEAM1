<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team1.health.dao.ReplyDAO">
  	<insert id="replyWrite">
  		insert into replyboard(reply_num, user_id, content)
  		values(#{reply_num}, #{user_id}, #{content})
  	</insert>
  	<select id="replyList" resultType="com.team1.health.vo.ReplyVO">
  		select reply_num, user_id, content, writedate
  		from replyboard where num=${param1}
  		order by reply_num desc
  	</select>
  	<update id="replyEdit">
  		update replyboard set content=#{content} where reply_num=${reply_num} and user_id=#{user_id}
  	</update>
  	<delete id="replyDel">
  		delete from replyboard where reply_num=${param1} and user_id=#{param2}
  	</delete>
</mapper>